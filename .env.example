# .env.example
# Copy to .env and set host permissions domains for production builds.
# Multiple domains separated by comma; wildcards supported
# Example: allow only company and API domains
PLASMO_PUBLIC_HOSTS=https://api.example.com/*,https://*.mycorp.com/*